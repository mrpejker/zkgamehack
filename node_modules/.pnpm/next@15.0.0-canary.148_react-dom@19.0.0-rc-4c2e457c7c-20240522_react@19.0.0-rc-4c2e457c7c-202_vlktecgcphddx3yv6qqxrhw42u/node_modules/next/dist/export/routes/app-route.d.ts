import type { ExportRouteResult, FileWriter } from '../types';
import type { IncrementalCache } from '../../server/lib/incremental-cache';
import type { MockedRequest, MockedResponse } from '../../server/lib/mock-request';
import type { ExperimentalConfig } from '../../server/config-shared';
import type { Params } from '../../client/components/params';
export declare const enum ExportedAppRouteFiles {
    BODY = "BODY",
    META = "META"
}
export declare function exportAppRoute(req: MockedRequest, res: MockedResponse, params: Params | undefined, page: string, incrementalCache: IncrementalCache | undefined, distDir: string, htmlFilepath: string, fileWriter: FileWriter, experimental: Required<Pick<ExperimentalConfig, 'after' | 'dynamicIO'>>): Promise<ExportRouteResult>;
