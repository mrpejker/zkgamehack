import { Batch, BatchStorage, HistoricalBatchStorage } from "@proto-kit/sequencer";
import { GraphqlModule } from "../GraphqlModule";
import { BlockModel, BlockResolver } from "./BlockResolver";
export declare class ComputedBlockModel {
    static fromServiceLayerModel({ bundles, proof }: Batch, blocks: (BlockModel | undefined)[]): ComputedBlockModel;
    bundles: BlockModel[];
    proof: string;
    constructor(bundles: BlockModel[], proof: string);
}
export declare class BatchStorageResolver extends GraphqlModule {
    private readonly batchStorage;
    private readonly unprovenResolver;
    constructor(batchStorage: BatchStorage & HistoricalBatchStorage, unprovenResolver: BlockResolver);
    batches(height: number | undefined): Promise<ComputedBlockModel | undefined>;
}
//# sourceMappingURL=BatchStorageResolver.d.ts.map