import { BatchStorage, BlockStorage, SettlementStorage } from "@proto-kit/sequencer";
export interface ProcessInformation {
    uptime: number;
    uptimeHumanReadable: string;
    headUsed: number;
    headTotal: number;
    nodeVersion: string;
    arch: string;
    platform: string;
}
export interface NodeInformation {
    blockHeight: number;
    batchHeight: number;
}
export declare class NodeStatusService {
    private readonly blockStorage;
    private readonly batchStorage;
    private readonly settlementStorage;
    constructor(blockStorage: BlockStorage, batchStorage: BatchStorage, settlementStorage: SettlementStorage);
    getProcessInfo(): ProcessInformation;
    getNodeInformation(): Promise<NodeInformation>;
}
//# sourceMappingURL=NodeStatusService.d.ts.map