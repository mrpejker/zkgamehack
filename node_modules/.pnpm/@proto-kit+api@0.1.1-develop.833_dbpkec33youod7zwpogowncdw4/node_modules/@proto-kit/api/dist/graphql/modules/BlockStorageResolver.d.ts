import { BlockStorage, HistoricalBlockStorage, ComputedBlock } from "@proto-kit/sequencer";
import { GraphqlModule } from "../GraphqlModule";
import { UnprovenBlockModel, UnprovenBlockResolver } from "./UnprovenBlockResolver";
export declare class ComputedBlockModel {
    static fromServiceLayerModel({ bundles, proof }: ComputedBlock, blocks: (UnprovenBlockModel | undefined)[]): ComputedBlockModel;
    bundles: UnprovenBlockModel[];
    proof: string;
    constructor(bundles: UnprovenBlockModel[], proof: string);
}
export declare class BlockStorageResolver extends GraphqlModule {
    private readonly batchStorage;
    private readonly unprovenResolver;
    constructor(batchStorage: BlockStorage & HistoricalBlockStorage, unprovenResolver: UnprovenBlockResolver);
    batches(height: number | undefined): Promise<ComputedBlockModel | undefined>;
}
//# sourceMappingURL=BlockStorageResolver.d.ts.map