import "reflect-metadata";
import { PublicKey, UInt64 } from "o1js";
import { RuntimeModule } from "@proto-kit/module";
import { Option, State, StateMap } from "@proto-kit/protocol";
export declare class Balances extends RuntimeModule<object> {
    /**
     * We use `satisfies` here in order to be able to access
     * presets by key in a type safe way.
     */
    static presets: {};
    balances: StateMap<PublicKey, UInt64>;
    totalSupply: State<any>;
    getBalance(address: PublicKey): Option<UInt64>;
    setBalance(address: PublicKey, balance: UInt64): void;
}
//# sourceMappingURL=graphql.d.ts.map