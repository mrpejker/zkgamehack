import { Field, PrivateKey, Signature } from "o1js";
import { AppChainModule } from "../appChain/AppChainModule";
export interface Signer {
    sign: (signatureData: Field[]) => Promise<Signature>;
}
export interface InMemorySignerConfig {
    signer: PrivateKey;
}
export declare class InMemorySigner extends AppChainModule<InMemorySignerConfig> implements Signer {
    constructor();
    sign(signatureData: Field[]): Promise<Signature>;
}
//# sourceMappingURL=InMemorySigner.d.ts.map