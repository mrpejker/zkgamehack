import { ProvableBlockHook } from "../protocol/ProvableBlockHook";
export class NoopBlockHook extends ProvableBlockHook {
    afterBlock(networkState, state) {
        return networkState;
    }
    beforeBlock(networkState, state) {
        return networkState;
    }
}
