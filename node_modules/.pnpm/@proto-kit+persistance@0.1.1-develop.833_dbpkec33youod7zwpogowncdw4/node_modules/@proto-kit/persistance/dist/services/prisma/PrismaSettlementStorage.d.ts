import { Settlement, SettlementStorage } from "@proto-kit/sequencer";
import type { PrismaConnection } from "../../PrismaDatabaseConnection";
import { SettlementMapper } from "./mappers/SettlementMapper";
export declare class PrismaSettlementStorage implements SettlementStorage {
    private readonly connection;
    private readonly settlementMapper;
    constructor(connection: PrismaConnection, settlementMapper: SettlementMapper);
    pushSettlement(settlement: Settlement): Promise<void>;
}
//# sourceMappingURL=PrismaSettlementStorage.d.ts.map