import { Batch, HistoricalBatchStorage, BatchStorage } from "@proto-kit/sequencer";
import type { PrismaConnection } from "../../PrismaDatabaseConnection";
import { BatchMapper } from "./mappers/BatchMapper";
export declare class PrismaBatchStore implements BatchStorage, HistoricalBatchStorage {
    private readonly connection;
    private readonly batchMapper;
    constructor(connection: PrismaConnection, batchMapper: BatchMapper);
    getBlockAt(height: number): Promise<Batch | undefined>;
    getCurrentBlockHeight(): Promise<number>;
    pushBlock(block: Batch): Promise<void>;
    getLatestBlock(): Promise<Batch | undefined>;
}
//# sourceMappingURL=PrismaBatchStore.d.ts.map