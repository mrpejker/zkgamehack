import { AsyncStateService, StateEntry } from "@proto-kit/sequencer";
import { Field } from "o1js";
import type { PrismaConnection } from "../../PrismaDatabaseConnection";
export declare class PrismaStateService implements AsyncStateService {
    private readonly connection;
    private readonly mask;
    private cache;
    /**
     * @param connection
     * @param mask A indicator to which masking level the values belong
     */
    constructor(connection: PrismaConnection, mask: string);
    commit(): Promise<void>;
    getMany(keys: Field[]): Promise<StateEntry[]>;
    openTransaction(): Promise<void>;
    get(key: Field): Promise<Field[] | undefined>;
    writeStates(entries: StateEntry[]): void;
}
//# sourceMappingURL=PrismaStateService.d.ts.map