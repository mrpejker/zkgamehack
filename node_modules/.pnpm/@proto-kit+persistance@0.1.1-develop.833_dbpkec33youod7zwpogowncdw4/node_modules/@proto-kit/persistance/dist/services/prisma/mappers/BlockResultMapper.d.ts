import { BlockResult } from "@proto-kit/sequencer";
import { BlockResult as DBBlockResult } from "@prisma/client";
import { ObjectMapper } from "../../../ObjectMapper";
import { StateTransitionArrayMapper } from "./StateTransitionMapper";
export declare class BlockResultMapper implements ObjectMapper<BlockResult, DBBlockResult> {
    private readonly stArrayMapper;
    constructor(stArrayMapper: StateTransitionArrayMapper);
    mapIn(input: DBBlockResult): BlockResult;
    mapOut(input: BlockResult): DBBlockResult;
}
//# sourceMappingURL=BlockResultMapper.d.ts.map