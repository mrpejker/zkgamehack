import { PendingTransaction, TransactionStorage } from "@proto-kit/sequencer";
import type { PrismaConnection } from "../../PrismaDatabaseConnection";
import { TransactionMapper } from "./mappers/TransactionMapper";
export declare class PrismaTransactionStorage implements TransactionStorage {
    private readonly connection;
    private readonly transactionMapper;
    constructor(connection: PrismaConnection, transactionMapper: TransactionMapper);
    getPendingUserTransactions(): Promise<PendingTransaction[]>;
    pushUserTransaction(tx: PendingTransaction): Promise<boolean>;
    findTransaction(hash: string): Promise<{
        transaction: PendingTransaction;
        block?: string;
        batch?: number;
    } | undefined>;
}
//# sourceMappingURL=PrismaTransactionStorage.d.ts.map