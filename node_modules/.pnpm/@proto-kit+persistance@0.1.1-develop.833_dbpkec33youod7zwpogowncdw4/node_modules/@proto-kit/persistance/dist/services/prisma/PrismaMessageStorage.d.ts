import { MessageStorage, PendingTransaction } from "@proto-kit/sequencer";
import type { PrismaConnection } from "../../PrismaDatabaseConnection";
import { TransactionMapper } from "./mappers/TransactionMapper";
export declare class PrismaMessageStorage implements MessageStorage {
    private readonly connection;
    private readonly transactionMapper;
    constructor(connection: PrismaConnection, transactionMapper: TransactionMapper);
    getMessages(fromMessageHash: string): Promise<PendingTransaction[]>;
    pushMessages(fromMessageHash: string, toMessageHash: string, messages: PendingTransaction[]): Promise<void>;
}
//# sourceMappingURL=PrismaMessageStorage.d.ts.map