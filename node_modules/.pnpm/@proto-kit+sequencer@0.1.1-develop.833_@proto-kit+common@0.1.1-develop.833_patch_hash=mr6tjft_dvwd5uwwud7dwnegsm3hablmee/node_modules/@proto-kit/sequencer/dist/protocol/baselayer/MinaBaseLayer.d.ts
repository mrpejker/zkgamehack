import { DependencyFactory } from "@proto-kit/common";
import { Mina } from "o1js";
import { MinaIncomingMessageAdapter } from "../../settlement/messages/MinaIncomingMessageAdapter";
import { SequencerModule } from "../../sequencer/builder/SequencerModule";
import { MinaTransactionSender } from "../../settlement/transactions/MinaTransactionSender";
import { WithdrawalQueue } from "../../settlement/messages/WithdrawalQueue";
import { BaseLayer } from "./BaseLayer";
export interface MinaBaseLayerConfig {
    network: {
        local: boolean;
        graphql?: string;
        archive?: string;
    };
}
export declare class MinaBaseLayer extends SequencerModule<MinaBaseLayerConfig> implements BaseLayer, DependencyFactory {
    network?: Parameters<typeof Mina.setActiveInstance>[0];
    dependencies(): {
        IncomingMessageAdapter: {
            useClass: typeof MinaIncomingMessageAdapter;
        };
        TransactionSender: {
            useClass: typeof MinaTransactionSender;
        };
        OutgoingMessageQueue: {
            useClass: typeof WithdrawalQueue;
        };
    };
    start(): Promise<void>;
}
//# sourceMappingURL=MinaBaseLayer.d.ts.map