import { type Field, Signature } from "o1js";
/**
 * CompressedSignature compresses the s scalar of a Signature
 * (which is expanded to 256 Fields in snarkyjs) to a single string
 */
export declare class CompressedSignature {
    readonly r: Field;
    readonly s: string;
    static fromSignature(sig: Signature): CompressedSignature;
    constructor(r: Field, s: string);
    toSignature(): Signature;
}
//# sourceMappingURL=CompressedSignature.d.ts.map