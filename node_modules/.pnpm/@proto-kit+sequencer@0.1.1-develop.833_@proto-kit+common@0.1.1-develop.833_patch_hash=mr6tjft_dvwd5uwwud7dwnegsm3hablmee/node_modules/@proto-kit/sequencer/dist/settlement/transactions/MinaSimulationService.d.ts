import { Field, Mina, PublicKey } from "o1js";
import { MinaBaseLayer } from "../../protocol/baselayer/MinaBaseLayer";
export declare class MinaSimulationService {
    private readonly baseLayer;
    constructor(baseLayer: MinaBaseLayer);
    private ledger;
    private networkState;
    private fetchGraphql;
    updateNetworkState(): Promise<void>;
    updateAccount(publicKey: PublicKey, tokenId?: Field): Promise<void>;
    applyTransaction(tx: Mina.Transaction<boolean, boolean>): void;
}
//# sourceMappingURL=MinaSimulationService.d.ts.map