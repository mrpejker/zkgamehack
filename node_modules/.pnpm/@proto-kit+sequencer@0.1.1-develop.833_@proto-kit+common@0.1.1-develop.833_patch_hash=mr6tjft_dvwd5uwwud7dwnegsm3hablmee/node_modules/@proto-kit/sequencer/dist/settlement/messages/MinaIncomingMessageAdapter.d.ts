import { PublicKey } from "o1js";
import { Runtime, RuntimeModulesRecord } from "@proto-kit/module";
import { PendingTransaction } from "../../mempool/PendingTransaction";
import type { MinaBaseLayer } from "../../protocol/baselayer/MinaBaseLayer";
import { IncomingMessageAdapter } from "./IncomingMessageAdapter";
/**
 * IncomingMessageAdapter implementation for a Mina Baselayer
 * based on decoding L1-dispatched actions
 */
export declare class MinaIncomingMessageAdapter implements IncomingMessageAdapter {
    private readonly baseLayer;
    private readonly runtime;
    constructor(baseLayer: MinaBaseLayer, runtime: Runtime<RuntimeModulesRecord>);
    private mapActionToTransactions;
    getPendingMessages(address: PublicKey, params: {
        fromActionHash: string;
        toActionHash?: string;
        fromL1Block: number;
    }): Promise<{
        from: string;
        to: string;
        messages: PendingTransaction[];
    }>;
}
//# sourceMappingURL=MinaIncomingMessageAdapter.d.ts.map