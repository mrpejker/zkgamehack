import { Closeable, TaskQueue } from "../queue/TaskQueue";
import { Task } from "../flow/Task";
export declare class FlowTaskWorker<Tasks extends Task<any, any>[]> implements Closeable {
    private readonly tasks;
    private readonly queue;
    private workers;
    constructor(mq: TaskQueue, tasks: Tasks);
    private initHandler;
    start(): Promise<void>;
    close(): Promise<void>;
}
//# sourceMappingURL=FlowTaskWorker.d.ts.map