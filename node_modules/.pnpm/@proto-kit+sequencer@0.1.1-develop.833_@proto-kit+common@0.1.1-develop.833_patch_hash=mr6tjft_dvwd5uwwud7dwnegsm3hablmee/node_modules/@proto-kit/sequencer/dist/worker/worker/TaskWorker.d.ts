import { MappingTask, MapReduceTask, ReducableTask } from "../manager/ReducableTask";
import { Closeable, TaskQueue } from "../queue/TaskQueue";
export declare class TaskWorker implements Closeable {
    private readonly tasks;
    private readonly queue;
    private workers;
    constructor(mq: TaskQueue);
    addReducableTask<Result>(queue: string, task: ReducableTask<Result>): void;
    addMapReduceTask<Input, Result>(queue: string, task: MapReduceTask<Input, Result>): void;
    addMapTask<Input, Result>(queue: string, task: MappingTask<Input, Result>): void;
    private initHandler;
    private doReduceStep;
    private doMapStep;
    start(): Promise<void>;
    close(): Promise<void>;
}
//# sourceMappingURL=TaskWorker.d.ts.map